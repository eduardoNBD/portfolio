---
export interface Props {
  color: string; 
  size?: string; 
}

const { color, size = 'text-xs' } = Astro.props as Props;
---
 
<span class={`bg-gray-100 text-gray-800 ${size} font-bold me-2 px-2.5 py-0.5 rounded dark:bg-${color}-900 dark:text-${color}-300 h-fit`}>
    <slot/>
</span> 